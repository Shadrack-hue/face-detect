# IntelStream AI Face Detection

A browser-based face detection demo that runs without a local Node.js build step.

## Run locally

Double-click the Windows launcher to start the app:

```text
start-server.bat
